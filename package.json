{
  "name": "npmo-docker",
  "version": "1.0.0",
  "description": "A docker setup for npmo",
  "scripts": {
    "build": "docker-compose -f roles/couchdb/couchdb.yml -f roles/registry/registry.yml -f roles/web/web.yml build",
    "configure": "./bin/npme-docker-compose.js configure",
    "up": "npm run build && docker-compose -f roles/couchdb/couchdb.yml -f roles/registry/registry.yml -f roles/web/web.yml up -d && docker ps",
    "stop": "docker-compose -f roles/couchdb/couchdb.yml -f roles/registry/registry.yml -f roles/web/web.yml stop && docker ps"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "inquirer": "^0.11.0",
    "lodash": "^3.10.1",
    "mustache": "^2.2.0",
    "request": "^2.65.0",
    "yargs": "^3.29.0"
  },
  "license": "ISC"
}
